import { useState } from 'react'
import { useAuth } from '../auth/AuthContext'
export default function Login() { const { loginAdmin, loginEmployee } = useAuth(), [accessId, setAccessId] = useState(''), [password, setPassword] = useState(''), [error, setError] = useState(''), [saving, setSaving] = useState(false); const submit = async event => { event.preventDefault(); setError(''); setSaving(true); try { await loginEmployee(accessId, password) } catch (reason) { setError(reason.message || 'Unable to sign in.') } finally { setSaving(false) } }; return <main className="login-page"><section className="login-card"><h1>Sign in</h1><p>Use your employee Access ID and password.</p><form onSubmit={submit}><label>Access ID<input value={accessId} onChange={event => setAccessId(event.target.value)} autoComplete="username" required/></label><label>Password<input type="password" value={password} onChange={event => setPassword(event.target.value)} autoComplete="current-password" required/></label>{error && <p className="login-error">{error}</p>}<button disabled={saving}>{saving ? 'Signing in...' : 'Employee sign in'}</button></form><button className="login-admin" onClick={loginAdmin}>Admin sign in</button></section></main> }