import { useEffect, useState } from 'react'
import { collection, doc, onSnapshot, setDoc, serverTimestamp, Timestamp, updateDoc } from 'firebase/firestore'
import { db } from '../../firebase/config'
import { createEmployee, generateAccessId, generatePassword, PERMISSIONS } from '../../utils/accessAccounts'
import './adminSettings.css'
const SETTINGS = doc(db, 'settings', 'inventory')
const isAdminAccount = account => account.role === 'superadmin'
export default function AdminSettings() {
  const [tab, setTab] = useState('access'), [accounts, setAccounts] = useState([]), [accessId, setAccessId] = useState(''), [password, setPassword] = useState(''), [showPassword, setShowPassword] = useState(false), [permissions, setPermissions] = useState([]), [error, setError] = useState(''), [saving, setSaving] = useState(false), [allowDimensionSizes, setAllowDimensionSizes] = useState(false), [createdCredentials, setCreatedCredentials] = useState(null), [copied, setCopied] = useState(false)
  useEffect(() => onSnapshot(collection(db, 'employeeProfiles'), snapshot => setAccounts(snapshot.docs.map(row => row.data()).sort((a,b) => (a.accessId || '').localeCompare(b.accessId || '')))), [])
  useEffect(() => onSnapshot(SETTINGS, snapshot => setAllowDimensionSizes(Boolean(snapshot.data()?.allowDimensionSizes))), [])
  const admins = accounts.filter(isAdminAccount), staff = accounts.filter(account => !isAdminAccount(account) && account.active)
  const permissionOwner = key => staff.find(account => account.permissions?.includes(key))
  const toggle = key => { setError(''); setPermissions(current => current.includes(key) ? current.filter(item => item !== key) : [...current, key]) }
  const copyCredentials = async () => { if (!createdCredentials) return; await navigator.clipboard.writeText(`Access ID: ${createdCredentials.accessId}\nPassword: ${createdCredentials.password}`); setCopied(true); window.setTimeout(() => setCopied(false), 1800) }
  const create = async event => { event.preventDefault(); setError(''); if (!permissions.length) { setError('Select at least one permission.'); return } setSaving(true); setCreatedCredentials(null); try { const result = await createEmployee({ accessId, password, permissions }); setCreatedCredentials({ accessId: result.accessId, password }); setAccessId(''); setPassword(''); setPermissions([]) } catch (reason) { setError(reason.message || 'Could not create employee.') } finally { setSaving(false) } }
  const updatePermissions = async (account, key) => { const owner = permissionOwner(key); if (!account.permissions?.includes(key) && owner && owner.uid !== account.uid) { setError(`${key} is already assigned to ${owner.accessId}.`); return } const next = account.permissions?.includes(key) ? account.permissions.filter(item => item !== key) : [...(account.permissions || []), key]; await updateDoc(doc(db, 'employeeProfiles', account.uid), { permissions: next, pendingAccessChange: true, updatedAt: Timestamp.now() }) }
  const deactivate = async account => { if (!window.confirm(`Deactivate ${account.accessId}? They will have two minutes to save work before logout.`)) return; await updateDoc(doc(db, 'employeeProfiles', account.uid), { active: false, accessRevokedAt: serverTimestamp(), updatedAt: serverTimestamp() }) }
  const permissionsView = (account, locked = false) => <div className="permission-list">{PERMISSIONS.map(([key,label]) => <label key={key}><input type="checkbox" disabled={locked} checked={locked || Boolean(account.permissions?.includes(key))} onChange={() => updatePermissions(account,key)}/>{label}</label>)}</div>
  return <section className="admin-settings"><header><h2>Settings</h2><p>Access control and Inventory format settings.</p></header><nav className="settings-tabs"><button className={tab==='access'?'active':''} onClick={() => setTab('access')}>Access Management</button><button className={tab==='sku'?'active':''} onClick={() => setTab('sku')}>Inventory SKU Format</button></nav>{tab === 'access' ? <><article className="access-create"><h3>Create employee Access ID</h3><p>Create one staff account with at least one assigned permission.</p><form onSubmit={create}><div className="credential-fields"><label>Access ID<input value={accessId} onChange={event => { setError(''); setAccessId(event.target.value) }} placeholder="e.g. rohan01" required/><button type="button" onClick={() => { setError(''); setAccessId(generateAccessId()) }}>Auto generate ID</button></label><label>Password<span className="password-control"><input type={showPassword ? 'text' : 'password'} value={password} onChange={event => { setError(''); setPassword(event.target.value) }} required/><button className="password-toggle" type="button" onClick={() => setShowPassword(current => !current)}>{showPassword ? 'Hide' : 'Show'}</button></span><button type="button" onClick={() => { setError(''); setPassword(generatePassword()) }}>Auto generate password</button></label></div><fieldset><legend>Permissions</legend>{PERMISSIONS.filter(([key]) => !permissionOwner(key)).map(([key, label]) => <label key={key}><input type="checkbox" checked={permissions.includes(key)} onChange={() => toggle(key)}/>{label}</label>)}</fieldset>{error && <p className="settings-error">{error}</p>}<button className="settings-primary" disabled={saving}>{saving ? 'Creating...' : 'Create Access ID'}</button></form></article><article className="access-list"><h3>Employee Accounts</h3>{staff.map(account => { const justCreated = createdCredentials?.accessId === account.accessId; return <div className="access-account staff-account" key={account.uid}><header><div><b>{account.accessId}</b></div><span>Active</span></header>{justCreated && <div className="new-staff-credentials"><div><small>New password</small><code>{createdCredentials.password}</code></div><button type="button" onClick={copyCredentials}>{copied ? 'Copied' : 'Copy ID and password'}</button></div>}<div className="staff-permissions"><small>Assigned permissions</small>{permissionsView(account)}</div><button className="settings-delete" onClick={() => deactivate(account)}>Deactivate account</button></div>})}{!staff.length && <p>No active employee Access IDs created yet.</p>}</article><article className="access-list admin-account-list"><h3>Admin Account</h3>{admins.map(account => <div className="access-account superadmin-account" key={account.uid}><header><div><b>{account.accessId}</b><small>Administrator</small></div><span>All access</span></header><p>This account always has every permission.</p>{permissionsView(account, true)}</div>)}</article></> : <article className="sku-settings"><div><h3>Dimension-style Inventory SKU format</h3><p>Allow Inventory sizes such as 4.3X2.0. This uses the existing Inventory setting.</p></div><label className="admin-toggle"><input type="checkbox" checked={allowDimensionSizes} onChange={event => setDoc(SETTINGS, { allowDimensionSizes: event.target.checked }, { merge: true })}/><span>{allowDimensionSizes ? 'Enabled' : 'Disabled'}</span></label></article>}</section>
}

