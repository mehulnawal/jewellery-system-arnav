import { useEffect, useMemo, useState } from 'react'
import { collection, onSnapshot, orderBy, query } from 'firebase/firestore'
import { db } from '../../firebase/config'
import { formatDecimal } from '../../utils/inventoryRules'
import './activityLog.css'
const PANELS = ['Inventory', 'Purchase', 'Challan']; const STAGES = ['Given for Sample', 'Return', 'Payment Pending (Follow-up)', 'Payment Complete']
const isoToday = () => new Date().toISOString().slice(0, 10); const asDate = (iso) => new Date(`${iso}T00:00:00`); const dayKey = (value) => { const date = value?.toDate?.() ?? new Date(value ?? 0); return Number.isNaN(date.getTime()) ? '' : date.toISOString().slice(0, 10) }; const time = (entry) => { const date = entry.createdAt?.toDate?.() ?? new Date(entry.createdAtMs ?? 0); return date.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit', hour12: true }) }; const money = (value) => `₹${Number(value || 0).toLocaleString('en-IN', { maximumFractionDigits: 2 })}`
export default function ActivityLog() { const [selectedDate, setSelectedDate] = useState(isoToday); const [entries, setEntries] = useState([]); const [loading, setLoading] = useState(true); const [open, setOpen] = useState({}); const today = isoToday(); useEffect(() => { const off = onSnapshot(query(collection(db, 'activityLog'), orderBy('createdAt', 'desc')), (snap) => { setEntries(snap.docs.map((item) => ({ id: item.id, ...item.data() }))); setLoading(false) }, () => setLoading(false)); return off }, []); const daily = useMemo(() => entries.filter((entry) => dayKey(entry.createdAt ?? entry.createdAtMs) === selectedDate).sort((a, b) => (b.createdAt?.toMillis?.() ?? b.createdAtMs ?? 0) - (a.createdAt?.toMillis?.() ?? a.createdAtMs ?? 0)), [entries, selectedDate]); const weight = daily.reduce((total, entry) => total + Number(entry.weight || 0), 0); const changeDay = (amount) => { const next = asDate(selectedDate); next.setDate(next.getDate() + amount); const value = next.toISOString().slice(0, 10); if (value <= today) setSelectedDate(value) }; const exportExcel = async () => { const XLSX = await import('xlsx'); const rows = daily.map((entry) => ({ Time: time(entry), Panel: entry.panel || '', Stage: entry.stage || '', Action: entry.action || '', Shape: entry.shape || '', Type: entry.type || '', 'Weight (ct)': entry.weight === '' || entry.weight == null ? '' : formatDecimal(entry.weight), SKU: entry.sku || '', 'Party name': entry.partyName || '', Amount: entry.amount || '', 'Challan no': entry.challanNo || '', 'Party no': entry.partyNo || '', ID: entry.recordId || entry.id })); const book = XLSX.utils.book_new(); XLSX.utils.book_append_sheet(book, XLSX.utils.json_to_sheet(rows), 'Activity Log'); const stamp = new Date().toISOString().replace(/[:T]/g, '-').slice(0, 16); XLSX.writeFile(book, `activityLog_${selectedDate}_${stamp}.xlsx`) }; const formatted = asDate(selectedDate).toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric', year: 'numeric' }); return <section className="activity-module"><header className="activity-heading"><div><h2>Activity Log</h2><p>Unified daily feed · Inventory, Purchase &amp; Challan</p></div><div className="activity-actions"><button onClick={() => window.print()}>⎙ Print</button><button onClick={exportExcel}>⇩ Export</button></div></header>{loading&&<div className="activity-empty activity-loading"><i/>Loading activity...</div>}<div className="activity-datebar"><button onClick={() => changeDay(-1)} aria-label="Previous day">‹</button><label><span>▣</span><input type="date" value={selectedDate} max={today} onChange={(event) => setSelectedDate(event.target.value)} /></label><button onClick={() => changeDay(1)} disabled={selectedDate >= today} aria-label="Next day">›</button><button className="activity-today" onClick={() => setSelectedDate(today)}>Today</button><strong>{formatted}</strong></div><div className="activity-metrics"><Metric label="ENTRIES" value={daily.length} /><Metric label="WEIGHT MOVED (CT)" value={`${formatDecimal(weight)} ct`} /></div><div className="activity-panels">{PANELS.map((panel) => panel === 'Challan' ? <Challan key={panel} entries={daily.filter((entry) => entry.panel === 'challan')} open={open} toggle={(key) => setOpen((state) => ({ ...state, [key]: !state[key] }))} /> : <Panel key={panel} panel={panel} entries={daily.filter((entry) => entry.panel === panel.toLowerCase())} isOpen={!!open[panel]} toggle={() => setOpen((state) => ({ ...state, [panel]: !state[panel] }))} />)}</div></section> }
function Metric({ label, value }) { return <article><span>{label}</span><strong>{value}</strong></article> } function Panel({ panel, entries, isOpen, toggle }) { return <article className="activity-panel"><button className={`activity-panel-head ${isOpen ? 'is-open' : ''}`} onClick={toggle}><i>›</i>{panel}<b>{entries.length}</b></button>{isOpen && <EntryList entries={entries} panel={panel.toLowerCase()} />}</article> } function Challan({ entries, open, toggle }) { const root = !!open.Challan; return <article className="activity-panel"><button className={`activity-panel-head ${root ? 'is-open' : ''}`} onClick={() => toggle('Challan')}><i>›</i>Challan<b>{entries.length}</b></button>{root && <div className="activity-stages">{STAGES.map((stage) => { const key = `stage-${stage}`; const stageEntries = entries.filter((entry) => entry.stage === stage); return <div key={stage}><button className={`activity-stage ${open[key] ? 'is-open' : ''}`} onClick={() => toggle(key)}><i>›</i>{stage}<b>{stageEntries.length}</b></button>{open[key] && <EntryList entries={stageEntries} panel="challan" stage={stage} />}</div> })}</div>}</article> } function EntryList({ entries, panel, stage }) { if (!entries.length) return <p className="activity-empty">No entries for this {stage ? 'stage' : 'panel'} on this day</p>; return <div className="activity-entries">{entries.map((entry) => <div className="activity-entry" key={entry.id}>{panel === 'inventory' ? <><time>{time(entry)}</time><span>{entry.shape} {entry.type}</span><span>{formatDecimal(entry.weight)} ct</span><b>{entry.sku}</b></> : panel === 'purchase' ? <><time>{time(entry)}</time><b>{entry.partyName}</b><span>{money(entry.amount)}</span><span>{formatDecimal(entry.weight)} ct</span><b>{entry.recordId || entry.id}</b></> : stage === 'Payment Complete' ? <><time>{time(entry)}</time><span>{money(entry.totalAmount ?? entry.amount)}</span><b>{entry.challanNo}</b><span>{entry.partyNo}</span></> : <><time>{time(entry)}</time><b>{entry.challanNo}</b><b>{entry.partyName}</b><span>{money(entry.amount)}</span></>}</div>)}</div> }
